# NormalizedContract

Contains root schema tree, normalized rules, source/schema metadata and rule-spec version. Schema nodes use absolute JSON Pointer paths. Rule scopes are expanded from local `$defs` scopes to active absolute paths during evaluation.
